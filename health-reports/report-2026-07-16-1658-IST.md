# AgadaHealth API Status — 2026-07-16 16:58 IST

## Endpoint checks

| Endpoint | Result | Details |
|---|---|---|
| `GET /agada-api/ping` | PASS | HTTP 200, application/json, status `OK` |
| `GET /agada-api/prices?q=paracetamol` | PASS | HTTP 200, valid JSON; Paracetamol 500mg, ₹2.50 per 10 tablets |
| `GET /agada-api/groq` | PASS | HTTP 200, valid JSON; route reports `configured: false` |

All requests used `Accept: application/json` to avoid the zo.space SPA fallback.

## Price verification

- Paracetamol: found, ₹2.50 per 10 tablets.
- Ibuprofen: found, ₹2.00 per 10 tablets.
- Azithromycin: found, ₹15.00 per 3 tablets.

## Groq image scan

The image payload test was BLOCKED with HTTP 500 before reaching Groq because no `AGADA_KEY_1` through `AGADA_KEY_6` secrets are configured in Zo Space. The request used the active vision model identifier `meta-llama/llama-4-scout-17b-16e-instruct` and an image_url content block, but upstream model execution could not be tested.

## Code and deployment checks

- Local `npm run build`: PASS.
- Zo Space route errors: none reported.
- Route code uses ESM `export default`.
- Active vision model is `llama-4-scout`, not the deprecated `llama-3.2-*-vision-preview`.
- Local price fallback is working.
- No code fix, restart, republish, or GitHub push was needed.

## Action required

Add at least one Groq key as `AGADA_KEY_1` in Zo Computer [Settings > Advanced > Secrets](/?t=settings&s=advanced). Until then, ping and price lookup are healthy, but AI image scans remain unavailable.
