# AgadaHealth API Status — 2026-07-17 16:58 IST

## Endpoint checks

| Endpoint | Result | Details |
|---|---|---|
| `GET /agada-api/ping` | PASS | HTTP 200, `application/json`, status `OK` |
| `GET /agada-api/prices?q=paracetamol` | PASS | HTTP 200, valid JSON; Paracetamol 500mg, ₹2.50 per 10 tablets |
| `GET /agada-api/groq` | PASS | HTTP 200, valid JSON; route reports `configured: false` |

All production checks used `Accept: application/json`. Without that header, zo.space serves the SPA HTML fallback for these paths; the API route itself is healthy.

## Price verification

- Paracetamol: found, ₹2.50 per 10 tablets.
- Ibuprofen: found, ₹2.00 per 10 tablets.
- Azithromycin: found, ₹15.00 per 3 tablets.

## Groq image scan

A real image-shaped medicine scan request was sent to `/agada-api/groq` using the active vision model `meta-llama/llama-4-scout-17b-16e-instruct`. It was blocked with HTTP 500 because no `AGADA_KEY_1` through `AGADA_KEY_6` secrets are configured in Zo Space. Therefore, a live AI response containing a medicine name and price could not be verified.

## Code and deployment checks

- Local `npm run build`: PASS.
- Zo Space route errors: none reported.
- Route code uses ESM `export default`.
- Active vision model is `llama-4-scout`, not the deprecated `llama-3.2-*-vision-preview`.
- Local price fallback is working.
- No code fix, restart, republish, or GitHub push was needed.

## Action required

Add at least one Groq key as `AGADA_KEY_1` in Zo Computer Settings → Advanced → Secrets. Until then, ping and local price lookup are healthy, but AI image scans remain unavailable.
